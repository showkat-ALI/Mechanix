import { useParams } from "react-router";

import React from 'react';

const Details = () => {
    return(
        <div>
            <h1>this is book</h1>
            </div>
    );
};

export default Details;